import 'package:flutter/material.dart';

class SearchAirport extends StatelessWidget {
  const SearchAirport({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Search Airport'),
      ),
    );
  }
}